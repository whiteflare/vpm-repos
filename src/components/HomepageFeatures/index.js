import clsx from 'clsx';
import Heading from '@theme/Heading';
import styles from './styles.module.css';
import IndexBody from './indexbody.mdx';

export default function HomepageFeatures() {
  return (
    <section className={styles.indexbody}>
      <IndexBody />
    </section>
  );
}
